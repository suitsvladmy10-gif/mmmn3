# Money Planner - Анализ банковских выписок

Приложение для автоматического анализа банковских выписок с использованием OCR распознавания, автоматической категоризации транзакций и интерактивного дашборда.

## Возможности

- 📤 **Загрузка файлов**: Drag & drop загрузка фото/PDF из личного кабинета банка
- 🔍 **OCR распознавание**: Автоматическое извлечение текста из изображений через Yandex Vision API
- 📊 **Парсинг данных**: Автоматическое извлечение транзакций для Сбербанка, Тинькофф и ВТБ
- 🏷️ **Категоризация**: Автоматическая классификация трат (Еда, Транспорт, Развлечения и т.д.)
- 📈 **Дашборд**: 
  - Общий баланс по всем банкам
  - Расходы по категориям (круговая диаграмма)
  - Тренд расходов (график по дням)
  - Топ-3 самых больших трат
  - Персональные рекомендации
- ✏️ **Редактирование**: Таблица со всеми транзакциями с возможностью редактирования и удаления
- 📥 **Экспорт**: Export PDF и Export JSON

## Технологический стек

- **Frontend**: Next.js 14+ (App Router), React, TypeScript, TailwindCSS, shadcn/ui
- **Backend**: Next.js API Routes
- **БД**: SQLite с Prisma ORM
- **OCR**: Yandex Vision API
- **AI Категоризация**: OpenAI GPT-4o mini (опционально)
- **Графики**: Recharts
- **Аутентификация**: NextAuth.js

## Установка

1. Клонируйте репозиторий или скачайте проект

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env.local
```

Отредактируйте `.env.local` и укажите:
```env
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-secret-key-here" # Сгенерируйте с помощью: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
YANDEX_VISION_API_KEY="your-yandex-vision-api-key"
YANDEX_FOLDER_ID="your-yandex-folder-id" # Опционально
OPENAI_API_KEY="your-openai-api-key" # Опционально, для AI категоризации
GEMINI_API_KEY="your-gemini-api-key" # Опционально, для AI категоризации
```

4. Настройте базу данных:
```bash
npx prisma generate
npx prisma migrate dev --name init
```

5. Запустите приложение:
```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## Получение API ключей

### Yandex Vision API

1. Зарегистрируйтесь в [Yandex Cloud](https://cloud.yandex.ru/)
2. Создайте сервисный аккаунт
3. Получите API ключ в разделе "Сервисные аккаунты"
4. Включите API Vision API для вашего облака

### OpenAI API (опционально)

1. Зарегистрируйтесь на [OpenAI](https://platform.openai.com/)
2. Создайте API ключ в разделе API Keys
3. Добавьте ключ в `.env.local`

## Использование

1. **Регистрация/Вход**: Создайте аккаунт или войдите в существующий
2. **Загрузка выписки**: Перейдите на страницу "Загрузить" и загрузите скриншот или PDF из личного кабинета банка
3. **Просмотр дашборда**: После загрузки транзакции автоматически появятся на дашборде
4. **Редактирование**: Перейдите на страницу "Транзакции" для просмотра, редактирования или удаления транзакций
5. **Экспорт**: Используйте кнопки "Export PDF" или "Export JSON" для экспорта данных

## Поддерживаемые банки

- Сбербанк
- Тинькофф
- ВТБ

## Структура проекта

```
/app
  /api          - API endpoints
  /dashboard    - Дашборд
  /transactions - Таблица транзакций
  /upload       - Загрузка файлов
/components    - React компоненты
/lib
  /parsers      - Парсеры для банков
  /categorization - Логика категоризации
/prisma        - Схема БД
```

## Разработка

```bash
# Запуск в режиме разработки
npm run dev

# Генерация Prisma клиента
npm run db:generate

# Миграции БД
npm run db:migrate

# Prisma Studio (GUI для БД)
npm run db:studio

# Сборка для продакшена
npm run build
npm start
```

## Деплой на Render

1. **Подготовьте репозиторий.** Закоммитьте код и `.env.example`, но не `.env.local`. Залейте проект на GitHub.
2. **Создайте Web Service на Render.**
   - Environment: Node 18+
   - Build Command: `npm install && npx prisma generate && npm run build`
   - Start Command: `npx prisma migrate deploy && npm run start`
3. **Настройте переменные окружения** (`DATABASE_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `YANDEX_VISION_API_KEY`, `YANDEX_FOLDER_ID`, `OPENAI_API_KEY`, `GEMINI_API_KEY`).
4. **База данных.** Для продакшена используйте Postgres (Render Managed PostgreSQL) и обновите `DATABASE_URL`. Выполните `prisma migrate deploy` перед первым запуском.
5. **Проверка.** После деплоя убедитесь, что OCR и AI ключи активны, а загрузка выписок работает.

## Лицензия

MIT
